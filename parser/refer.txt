# ============ NAVIGATION ============
MOVE_CURSOR(unit, count, direction)  # unit: char|word|line|block
JUMP_TO_LINE(line_number)
JUMP_TO_COLUMN(column)
JUMP_TO_DEFINITION(symbol)
JUMP_TO_SCOPE(type, direction)  # type: function|class|bracket
JUMP_TO_MATCHING_BRACKET()
JUMP_TO_CHAR(char, direction)
GO_TO_DEFINITION()
GO_TO_REFERENCES()
GO_BACK()  # Navigation history
GO_FORWARD()
SCROLL(direction, amount)

# ============ SELECTION ============
SELECT_SCOPE(type)  # word|line|block|function|class|brackets
SELECT_RANGE(start_line, end_line)
SELECT_NEXT(unit, count)
SELECT_PREVIOUS(unit, count)
SELECT_UNTIL(char, include)
SELECT_INSIDE(delimiter)
SELECT_AROUND(delimiter)
EXPAND_SELECTION()
SHRINK_SELECTION()
EXTEND_SELECTION(unit, count, direction)
SELECT_ALL_OCCURRENCES(text)
SELECT_MATCHING(pattern)
ADD_CURSOR(position)
CLEAR_CURSORS()

# ============ EDITING - Basic ============
INSERT_TEXT(text)
INSERT_SNIPPET(name, params)
DELETE_SELECTION()
DELETE_LINE(offset)
DELETE_LINES(count, direction)
DELETE_WORD(direction)
DELETE_TO_END_OF_LINE()
DELETE_SCOPE(type)
COPY(target)
CUT(target)
PASTE(position)
DUPLICATE(target, direction)
UNDO()
REDO()

# ============ EDITING - Structural ============
WRAP_WITH(wrapper, params)
EXTRACT_VARIABLE(name)
EXTRACT_FUNCTION(name)
INLINE_VARIABLE()
INLINE_FUNCTION()
MOVE_LINES(direction, count)
SPLIT_LINE(at)
JOIN_LINES(count)
INDENT(direction, target)
COMMENT_OUT(target)
UNCOMMENT(target)
TOGGLE_COMMENT(target)

# ============ FIND/REPLACE ============
FIND(text, scope)
FIND_NEXT()
FIND_PREVIOUS()
REPLACE(old_text, new_text, scope)
REPLACE_ALL(old_text, new_text)

# ============ CODE ACTIONS ============
RENAME_SYMBOL(new_name)
ADD_IMPORT(library_name)
ORGANIZE_IMPORTS()
FORMAT_DOCUMENT()
FORMAT_SELECTION()
FIX_LINT_ERRORS(scope)
FOLD(scope)
UNFOLD(scope)
TOGGLE_FOLD(scope)

# ============ FILE/PROJECT ============
FILE_ACTION(action)  # save|close|create|rename
OPEN_FILE(path)
SWITCH_TO_FILE(name)
SWITCH_TO_RECENT_FILE(index)

# ============ ENVIRONMENT ============
RUN_COMMAND(cmd)
TERMINAL_RUN(command)
TERMINAL_CLEAR()
SPLIT_EDITOR(direction)
FOCUS_EDITOR(position)
TOGGLE_PANEL(panel)
TOGGLE_SIDEBAR()

# ============ UTILITY ============
SHOW_SUGGESTIONS()
CONFIRM_LAST_ACTION()
CANCEL_COMMAND()
REPEAT_LAST_COMMAND()
DESCRIBE_CONTEXT()